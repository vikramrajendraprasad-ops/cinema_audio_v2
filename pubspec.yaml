name: cinema_audio_v2
description: "A cinematic audio DSP converter app using FFmpeg."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  ffmpeg_kit_flutter_new_audio: ^2.0.0  # Updated to valid version
  file_picker: ^8.1.2  # For picking audio files
  path_provider: ^2.1.4  # For saving output files

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
